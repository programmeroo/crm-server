<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pi CRM</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900">
    <nav class="bg-blue-600 p-4 text-white shadow-md">
        <div class="max-w-4xl mx-auto flex justify-between items-center">
            <a href="/" class="font-bold text-xl">Pi-CRM</a>
            <div class="flex items-center space-x-4">
                <% if (typeof user !== 'undefined' && user) { %>
                    <%- include('workspace-switcher') %>
                    <a href="/contacts" class="hover:underline">Contacts</a>
                    <span class="text-blue-200 text-sm"><%= user.display_name %></span>
                    <form action="/logout" method="POST" class="inline">
                        <button type="submit" class="text-sm bg-blue-700 hover:bg-blue-800 px-3 py-1 rounded transition">Logout</button>
                    </form>
                <% } %>
            </div>
        </div>
    </nav>
    <main class="max-w-4xl mx-auto mt-8 p-4">
        <%- include('flash-messages', { success: typeof success !== 'undefined' ? success : [], error: typeof error !== 'undefined' ? error : [] }) %>
